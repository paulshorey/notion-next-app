import { Global, css, useTheme } from '@emotion/react';
// import html from 'src/emotion/global/html';
// import classes from 'src/emotion/global/classes';
// import fonts from 'src/emotion/global/fonts';

const Index = () => {
  // let theme = useTheme();
  return <Global styles={css``} />;
};

export default Index;

// @import url('/icomoon/style.css');
// ${html(theme)}
// ${classes(theme)}
// ${fonts(theme)}
